# API calls use relative paths in dev — Vite proxy forwards /api to localhost:3001
# Socket.IO also proxied via /socket.io → localhost:3001 (with ws:true for WebSocket)
# (see vite.config.ts server.proxy)
