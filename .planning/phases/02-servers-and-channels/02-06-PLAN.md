---
phase: 02-servers-and-channels
plan: 06
type: execute
wave: 3
depends_on: ["02-03", "02-04", "02-05"]
files_modified:
  - apps/client/src/pages/server/settings/ServerSettings.tsx
  - apps/client/src/pages/server/settings/OverviewTab.tsx
  - apps/client/src/pages/server/settings/InvitesTab.tsx
  - apps/client/src/pages/server/settings/MembersTab.tsx
  - apps/client/src/pages/server/settings/ChannelsTab.tsx
  - apps/client/src/components/server/InviteModal.tsx
  - apps/client/src/components/server/MemberList.tsx
  - apps/client/src/App.tsx
autonomous: false

must_haves:
  truths:
    - "Server owner can access full-page settings with Overview, Invites, Members, Channels tabs"
    - "Owner can edit server name from the Overview tab"
    - "Owner can create invites with expiry/max-use options and see all active invites"
    - "Owner can revoke invites and kick members from settings"
    - "Owner can delete server after typing server name to confirm"
    - "Any member can leave a server from the settings or a context option"
    - "Quick invite modal accessible from channel panel header"
    - "Toggleable member list panel on the right side of the chat area"
  artifacts:
    - path: "apps/client/src/pages/server/settings/ServerSettings.tsx"
      provides: "Full-page settings layout with sidebar navigation"
    - path: "apps/client/src/pages/server/settings/OverviewTab.tsx"
      provides: "Server name edit, delete server with confirmation"
    - path: "apps/client/src/pages/server/settings/InvitesTab.tsx"
      provides: "Invite creation form with expiry/max-use, invite list with revoke"
    - path: "apps/client/src/pages/server/settings/MembersTab.tsx"
      provides: "Searchable member list with kick button for owner"
    - path: "apps/client/src/components/server/InviteModal.tsx"
      provides: "Quick invite creation modal with copy-to-clipboard"
    - path: "apps/client/src/components/server/MemberList.tsx"
      provides: "Right-side toggleable member panel for chat view"
  key_links:
    - from: "apps/client/src/pages/server/settings/InvitesTab.tsx"
      to: "/api/servers/:id/invites"
      via: "useQuery + useMutation for invite CRUD"
      pattern: "api\\.(get|post|delete).*invites"
    - from: "apps/client/src/pages/server/settings/MembersTab.tsx"
      to: "/api/servers/:id/members"
      via: "useServerMembers + kick mutation"
      pattern: "api\\.delete.*members"
---

<objective>
Server settings, invite management, and member list: the server administration and social features.

Purpose: Owners need to manage their servers — edit settings, create/revoke invites, kick members, delete the server. Members need to see who else is in the server. The invite modal provides quick access to sharing invites.

Output: Full-page server settings with 4 tabs, quick invite modal, toggleable member list panel.
</objective>

<execution_context>
@C:\Users\maxi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\maxi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-servers-and-channels/02-CONTEXT.md
@.planning/phases/02-servers-and-channels/02-RESEARCH.md
@.planning/phases/02-servers-and-channels/02-03-SUMMARY.md
@.planning/phases/02-servers-and-channels/02-05-SUMMARY.md
@apps/client/src/hooks/useServers.ts
@apps/client/src/hooks/useChannels.ts
@apps/client/src/pages/server/ServerView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server settings page with Overview, Invites, Members, and Channels tabs</name>
  <files>
    apps/client/src/pages/server/settings/ServerSettings.tsx
    apps/client/src/pages/server/settings/OverviewTab.tsx
    apps/client/src/pages/server/settings/InvitesTab.tsx
    apps/client/src/pages/server/settings/MembersTab.tsx
    apps/client/src/pages/server/settings/ChannelsTab.tsx
    apps/client/src/App.tsx
  </files>
  <action>
    1. Create `apps/client/src/pages/server/settings/ServerSettings.tsx`:
       - Full-page settings view (replaces the server content area, not a modal)
       - Layout: sidebar navigation on the left (list of tabs), content area on the right
       - Sidebar: "Overview", "Invites", "Members", "Channels" tabs + "Leave Server" button at the bottom (red text)
       - Active tab highlighted
       - "X" close button in the top-right corner — navigates back to `/servers/${serverId}`
       - Uses local state (useState) to track active tab, renders corresponding tab component
       - Get `serverId` from `useParams()`
       - Fetch server data with `useQuery({ queryKey: ["servers", serverId], queryFn: () => api.get(\`/api/servers/${serverId}\`) })`
       - Dark theme: bg-zinc-900 for sidebar, bg-zinc-800 for content area
       - "Leave Server" click: confirmation dialog ("Are you sure you want to leave {serverName}?"), on confirm: `api.delete(\`/api/servers/${serverId}/members/${userId}\`)`, emit `socket.emit("server:unsubscribe", { serverId })`, invalidate servers query, navigate to `/`

    2. Create `apps/client/src/pages/server/settings/OverviewTab.tsx`:
       - Props: `server: ServerResponse`
       - Server name input (pre-filled with current name) + "Save Changes" button
       - Uses mutation: `api.patch(\`/api/servers/${server.id}\`, { name })` with invalidateQueries on success
       - "Delete Server" section at the bottom (danger zone):
         - Red-bordered section with warning text
         - "Delete Server" button opens confirmation dialog
         - Confirmation requires typing the server name exactly (case-sensitive)
         - On confirm: `api.delete(\`/api/servers/${server.id}\`)`, navigate to `/`, invalidate servers query
         - Only visible if user is server owner

    3. Create `apps/client/src/pages/server/settings/InvitesTab.tsx`:
       - "Create Invite" section at top:
         - Expiry dropdown: 30 minutes, 1 hour, 6 hours, 12 hours, 24 hours, 7 days, Never (map to seconds for the API)
         - Max uses dropdown: 1, 5, 10, 25, 50, 100, No limit (null)
         - "Generate" button creates invite via `api.post(\`/api/servers/${serverId}/invites\`, { expiresIn, maxUses })`
       - Active invites list:
         - Fetch with `useQuery({ queryKey: ["servers", serverId, "invites"], queryFn: () => api.get(\`/api/servers/${serverId}/invites\`) })`
         - Each row: invite code (monospace), creator name, uses/maxUses, expiry time (relative, e.g., "expires in 6 hours" or "Never expires"), "Copy Link" button, "Revoke" button (owner only)
         - "Copy Link" copies `${window.location.origin}/invite/${code}` to clipboard
         - "Revoke" calls `api.delete(\`/api/servers/${serverId}/invites/${inviteId}\`)`, invalidates query

    4. Create `apps/client/src/pages/server/settings/MembersTab.tsx`:
       - Uses `useServerMembers(serverId)` hook
       - Search input at top to filter members by name
       - Member list: avatar (colored initial circle using same `stringToHue`), display name, joined date
       - If current user is server owner: "Kick" button (X icon) next to each member (except themselves)
       - Kick calls `api.delete(\`/api/servers/${serverId}/members/${userId}\`)`, invalidates members query
       - Owner badge next to the server owner's name

    5. Create `apps/client/src/pages/server/settings/ChannelsTab.tsx`:
       - Lists all channels grouped by type
       - For each channel: name input (inline edit), type badge, "Delete" button
       - "Add Channel" button at top with name input and type selector (text/voice)
       - Uses `useCreateChannel`, `useUpdateChannel`, `useDeleteChannel` hooks
       - Simpler than the main channel list — no drag-and-drop here (that's in the sidebar). This is a flat management list with edit/delete controls. Up/down arrow buttons for reorder (accessibility alternative per CONTEXT.md).

    6. Add route in `apps/client/src/App.tsx`:
       - Add a nested route under the server route:
         ```tsx
         <Route path="servers/:serverId/settings" element={<ServerSettings />} />
         ```
       - This should be inside the AppShell layout route (the server icon strip remains visible during settings)

    IMPORTANT: Wire the server name header dropdown in ChannelList (from 02-05) to include "Server Settings" and "Invite People" options. If the dropdown wasn't created in 02-05, add it now — clicking the server name at the top of the channel panel opens a dropdown with at minimum: "Invite People" (opens InviteModal), "Server Settings" (navigates to /servers/:serverId/settings), and "Leave Server" (same leave flow as settings sidebar).
  </action>
  <verify>
    Run `cd apps/client && npx tsc --noEmit` — zero TypeScript errors.
    Run `cd apps/client && npx vite build` — build succeeds.
  </verify>
  <done>
    - Server settings page accessible from channel panel header dropdown
    - Overview tab: edit server name, delete server with name-confirmation
    - Invites tab: create with expiry/max-use, list active invites, copy link, revoke
    - Members tab: searchable list with kick (owner only)
    - Channels tab: add/edit/delete channels with up/down reorder buttons
    - Leave server from settings sidebar with confirmation
  </done>
</task>

<task type="auto">
  <name>Task 2: Quick invite modal and toggleable member list panel</name>
  <files>
    apps/client/src/components/server/InviteModal.tsx
    apps/client/src/components/server/MemberList.tsx
    apps/client/src/pages/server/ServerView.tsx
  </files>
  <action>
    1. Create `apps/client/src/components/server/InviteModal.tsx`:
       - Quick invite dialog triggered from channel panel header dropdown ("Invite People")
       - Simplified version of InvitesTab:
         - Auto-generates an invite with default settings (24h expiry, no use limit) on open
         - Shows the invite link in a copyable input: `${window.location.origin}/invite/${code}`
         - "Copy" button with visual feedback (changes to "Copied!" for 2 seconds)
         - Optional: expiry and max-use dropdowns for customization (same options as InvitesTab)
       - Uses useMutation to create the invite on modal open
       - Dark theme, consistent with CreateServerModal styling

    2. Create `apps/client/src/components/server/MemberList.tsx`:
       - Right-side panel (w-60) showing server members
       - Uses `useServerMembers(serverId)` hook
       - Groups members by online/offline status (for now, just show all members since presence is Phase 4 — label section as "Members — N")
       - Each member: colored initial avatar (same `stringToHue` on user ID), display name
       - Owner has a crown/shield badge
       - Dark theme (bg-zinc-800 or bg-zinc-850, slightly lighter than main content area)
       - Scrollable if many members (overflow-y-auto)

    3. Update `apps/client/src/pages/server/ServerView.tsx`:
       - Add state to toggle member list: `const [showMembers, setShowMembers] = useState(false)`
       - Add a header bar at the top of the main content area:
         - Channel name (# channel-name) on the left
         - Toggle member list button (people icon) on the right — toggles `showMembers`
       - Layout when members visible: channel panel (fixed) + main content (flex-1) + member list (fixed w-60)
       - Layout when members hidden: channel panel (fixed) + main content (flex-1)
       - The main content area still renders `<Outlet />` for the channel content (Phase 3)
       - Member list slides in/out smoothly (or just shows/hides — smooth animation is a nice-to-have)

    IMPORTANT: The member list panel is part of the 4-column layout described in CONTEXT.md: icon strip | channel panel | chat area | member panel. The member panel is the rightmost column and is toggleable.
  </action>
  <verify>
    Run `cd apps/client && npx tsc --noEmit` — zero TypeScript errors.
    Run `cd apps/client && npx vite build` — build succeeds.
  </verify>
  <done>
    - Quick invite modal auto-generates invite with copy-to-clipboard
    - Member list panel shows all server members with avatars
    - Member list toggleable via button in content header
    - 4-column layout works when member list is open
    - Visual consistency across all new components
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Server settings (4 tabs), quick invite modal, toggleable member list, server header dropdown, leave server flow, delete server with confirmation</what-built>
  <how-to-verify>
    1. Click the server name at the top of the channel panel — dropdown should appear with "Invite People", "Server Settings", "Leave Server"
    2. Click "Invite People" — invite modal with a generated link and copy button
    3. Click "Server Settings" — full-page settings with Overview, Invites, Members, Channels tabs
    4. Overview: change server name, verify it updates in the sidebar
    5. Invites: create an invite with custom expiry, verify it appears in the list
    6. Copy an invite link, open in an incognito window — should redirect to login then back to invite
    7. Members: verify member list shows, search works
    8. Click the people icon in the content header — member list panel should toggle on the right
    9. Create a second user, join via invite — verify the new member appears in the member list in real-time (Socket.IO)
    10. As owner: kick the second user from Members tab — they should be removed
    11. As a non-owner member: verify "Leave Server" works
    12. Delete server flow: type server name to confirm, verify redirect to home
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `cd apps/client && npx tsc --noEmit` passes
- `cd apps/client && npx vite build` succeeds
- Full settings flow works: edit name, create/revoke invites, kick members, delete server
- Invite modal generates link and copies to clipboard
- Member list toggles on/off cleanly
- Leave server removes user and updates UI in real-time
</verification>

<success_criteria>
Server management is complete: settings with 4 functional tabs, quick invite sharing, member visibility, leave/kick/delete flows. All real-time updates via Socket.IO cache invalidation. Phase 2 success criteria are fully met.
</success_criteria>

<output>
After completion, create `.planning/phases/02-servers-and-channels/02-06-SUMMARY.md`
</output>
