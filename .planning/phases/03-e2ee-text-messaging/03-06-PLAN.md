---
phase: 03-e2ee-text-messaging
plan: 06
type: execute
wave: 3
depends_on: ["03-03", "03-04"]
files_modified:
  - apps/client/src/pages/dm/DMLayout.tsx
  - apps/client/src/pages/dm/DMList.tsx
  - apps/client/src/pages/dm/DMView.tsx
  - apps/client/src/components/server/ServerList.tsx
  - apps/client/src/components/server/ChannelList.tsx
  - apps/client/src/App.tsx
autonomous: true
requirements:
  - DM-01
  - DM-02

must_haves:
  truths:
    - "User can click the DM icon at the top of the server strip to open the DM sidebar"
    - "User can see a list of DM conversations sorted by most recent activity"
    - "User can open a DM conversation and see the same message UI as text channels"
    - "User can start a DM from a context menu on a member in the member list"
    - "User can start a new DM from a button in the DM list sidebar"
  artifacts:
    - path: "apps/client/src/pages/dm/DMLayout.tsx"
      provides: "DM layout with sidebar + conversation area"
      exports: ["default"]
    - path: "apps/client/src/pages/dm/DMList.tsx"
      provides: "DM conversation list sorted by recency"
      exports: ["default"]
    - path: "apps/client/src/pages/dm/DMView.tsx"
      provides: "DM conversation view (reuses chat components)"
      exports: ["default"]
  key_links:
    - from: "apps/client/src/components/server/ServerList.tsx"
      to: "apps/client/src/pages/dm/DMLayout.tsx"
      via: "DM icon in server strip navigates to /dms route"
      pattern: "navigate.*dms"
    - from: "apps/client/src/components/server/ChannelList.tsx"
      to: "apps/client/src/hooks/useDMs.ts"
      via: "Context menu 'Message' option on member calls useCreateDM"
      pattern: "useCreateDM|Message"
---

<objective>
Build the DM UI: DM icon in the server strip, DM sidebar with conversation list, DM conversation view reusing chat components, and start-DM entry points (member context menu + new-DM button).

Purpose: DMs are a core communication feature. This plan creates the navigation and UI for 1:1 encrypted conversations, reusing the message components from 03-05.

Output: Complete DM navigation and conversation experience.
</objective>

<execution_context>
@/home/maxi/.claude/get-shit-done/workflows/execute-plan.md
@/home/maxi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-e2ee-text-messaging/03-CONTEXT.md
@.planning/phases/03-e2ee-text-messaging/03-RESEARCH.md
@.planning/phases/03-e2ee-text-messaging/03-03-SUMMARY.md
@.planning/phases/03-e2ee-text-messaging/03-04-SUMMARY.md
@.planning/phases/03-e2ee-text-messaging/03-05-SUMMARY.md

@apps/client/src/components/server/ServerList.tsx
@apps/client/src/components/server/ChannelList.tsx
@apps/client/src/components/chat/MessageList.tsx
@apps/client/src/components/chat/MessageInput.tsx
@apps/client/src/App.tsx
@apps/client/src/hooks/useDMs.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DM layout, list, and conversation view</name>
  <files>
    apps/client/src/pages/dm/DMLayout.tsx
    apps/client/src/pages/dm/DMList.tsx
    apps/client/src/pages/dm/DMView.tsx
  </files>
  <action>
**1. Create `DMLayout.tsx`** — Layout component for the DM section:

- Similar structure to `ServerView`: sidebar (w-60) + Outlet for conversation
- Sidebar contains `DMList`
- Header at top of sidebar: "Direct Messages" title
- `<Outlet />` fills the remaining space for the active DM conversation
- When no DM is selected (index route), show a centered placeholder: "Select a conversation or start a new one"

**2. Create `DMList.tsx`** — DM conversation list sidebar:

- Uses `useDMs()` hook to fetch conversations
- Renders each conversation as a row: avatar circle (with initials, same as ServerIcon pattern) + display name + last message preview time
- Sorted by most recent activity (already sorted by API)
- **Selected state**: highlight the active DM (match from URL params)
- **New DM button** at the top: Opens a dialog to select a user to message. This dialog:
  - Shows a search/select UI to pick from users the current user shares a server with
  - To get this list: iterate through all servers the user is in, collect unique members from server members queries
  - On select: call `useCreateDM().mutate(recipientUserId)` → navigate to `/dms/${channelId}`
  - Use radix-ui Dialog for the modal
- **Navigation**: clicking a conversation navigates to `/dms/${channelId}`
- **Empty state**: "No conversations yet. Start a DM from a member list or the + button above."

**3. Create `DMView.tsx`** — DM conversation view:

- Gets `channelId` from `useParams()`
- Reuses `MessageList` and `MessageInput` from `components/chat/`
- **Header**: Shows the other participant's avatar + display name (from `useDMs()` data or a dedicated query)
- **Send handler**: When sending a DM message, the recipients list is exactly 2: self (from useAuth) and the other participant (from DM conversation data)
- Same crypto unlock check as ChannelView — show CryptoUnlockPrompt if keys not cached
- Layout is identical to ChannelView: header + message list + input
  </action>
  <verify>
    <automated>cd /home/maxi/Documents/coding/Projects/Tether && npx turbo typecheck --filter=@tether/client 2>&1 | tail -5</automated>
  </verify>
  <done>DMLayout provides sidebar + outlet. DMList shows conversations sorted by recency with new-DM button. DMView reuses chat components for 1:1 conversations. User can start new DMs from the DM list.</done>
</task>

<task type="auto">
  <name>Task 2: DM navigation (server strip icon, member context menu, routes)</name>
  <files>
    apps/client/src/components/server/ServerList.tsx
    apps/client/src/components/server/ChannelList.tsx
    apps/client/src/App.tsx
  </files>
  <action>
**1. Add DM icon to ServerList.tsx** (per CONTEXT.md: "DM icon at the top of the server icon strip, like Discord's home icon"):

- Add a DM/Home icon at the very top of the server strip, before the divider
- This is separate from the existing brand icon — it serves as the DM navigation entry point
- Icon: A speech bubble or chat icon SVG (Claude's discretion on exact icon design)
- Clicking navigates to `/dms`
- Active state: highlighted when current path starts with `/dms`
- Same pill indicator and hover styling as server icons (rounded-square morph, etc.)

**2. Add "Message" context menu option** to member items in `ChannelList.tsx` (or the member list panel if it exists):

- On the member list panel (the toggleable panel from Phase 2), each member item should have a right-click context menu
- Add a "Message" option that:
  - Calls `useCreateDM().mutateAsync(member.userId)` to find-or-create the DM channel
  - Navigates to `/dms/${channelId}` with the returned channel ID
- Use radix-ui ContextMenu (already available)
- Only show "Message" option for other users (not self)

NOTE: If the member list is part of a separate component (e.g., `MemberList.tsx` inside `ChannelList.tsx` or `ServerView.tsx`), add the context menu there instead. Read the actual component structure from 03-05-SUMMARY.md to determine where member items are rendered.

**3. Update `App.tsx`** — Add DM routes:

```tsx
import DMLayout from "@/pages/dm/DMLayout";
import DMView from "@/pages/dm/DMView";

// Inside the AppShell protected route children:
<Route path="dms" element={<DMLayout />}>
  <Route index element={/* placeholder */} />
  <Route path=":channelId" element={<DMView />} />
</Route>
```

The DM routes are siblings to the `servers/:serverId` route, both under the AppShell layout.
  </action>
  <verify>
    <automated>cd /home/maxi/Documents/coding/Projects/Tether && npx turbo typecheck --filter=@tether/client 2>&1 | tail -5</automated>
  </verify>
  <done>DM icon in server strip navigates to /dms. Member context menu has "Message" option that creates/opens DM. DM routes wired in App.tsx under AppShell. Full DM navigation flow works: icon → DM list → DM conversation.</done>
</task>

</tasks>

<verification>
- `npx turbo typecheck` passes for client
- DM icon visible at top of server strip
- Clicking DM icon navigates to /dms showing conversation list
- Clicking a conversation opens the DM view with message history
- "Message" context menu on members creates DM and navigates
- New-DM button in DM list opens user selection dialog
</verification>

<success_criteria>
1. DM icon at top of server strip (before divider) navigates to /dms
2. DM list shows conversations sorted by most recent message
3. Can open a DM conversation and see decrypted message history
4. Can send encrypted messages in a DM (exactly 2 recipients: self + other)
5. Can start a new DM from the DM list via + button with user selection
6. Can start a DM from right-clicking a member ("Message" option)
7. DM navigation coexists with server navigation (both under AppShell)
</success_criteria>

<output>
After completion, create `.planning/phases/03-e2ee-text-messaging/03-06-SUMMARY.md`
</output>
