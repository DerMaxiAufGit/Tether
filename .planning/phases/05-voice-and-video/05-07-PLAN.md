---
phase: 05-voice-and-video
plan: 7
type: execute
wave: 6
depends_on: ["05-06"]
files_modified: []
autonomous: false
requirements: [CHAN-03, VOICE-01, VOICE-02, VOICE-03, VOICE-04]

must_haves:
  truths:
    - "User can join a voice channel and hear other participants via WebRTC P2P audio through Coturn TURN relay"
    - "User can mute and deafen, visible to other participants"
    - "User can enable camera and other participants see the video feed"
    - "User can share their screen and all participants see it"
    - "Voice activity indicator lights up when participants speak"
    - "ICE candidate exchange does not begin until the call is explicitly accepted"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for the complete voice and video system.

Purpose: Validate that all voice/video functionality works end-to-end with real WebRTC connections through Coturn. This cannot be automated — it requires two browser windows with real audio/video devices.

Output: User approval confirming phase 5 success criteria are met.
</objective>

<execution_context>
@/home/maxi/.claude/get-shit-done/workflows/execute-plan.md
@/home/maxi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-voice-and-video/05-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete voice and video functionality</name>
  <files>N/A — verification only</files>
  <action>
Human verifies the complete voice and video system by running through 9 test scenarios covering voice join, mute/deafen, camera, screen share, voice activity, PiP, connection quality, auto-switch, and disconnect.

What was built: Complete voice and video system — TURN credential endpoint (HMAC-SHA1), Socket.IO signaling (join, leave, offer, answer, ICE relay), WebRTC P2P mesh with perfect negotiation, mute/deafen controls, voice activity detection (AnalyserNode), camera toggle (replaceTrack), screen share (addTrack + renegotiation), voice channel UI (participant grid, speaking indicators, PiP), voice controls in user info bar, connection quality indicator.
  </action>
  <verify>
    Start the dev environment: `cd /home/maxi/Documents/coding/Projects/Tether && docker compose up -d && cd apps/server && pnpm dev` (in one terminal) and `cd apps/client && pnpm dev` (in another).

    Open two browser windows (or one regular + one incognito) logged in as different users in the same server.

    **Test 1 — Voice channel join (CHAN-03):**
    - In Window A, click a voice channel in the sidebar
    - Grant microphone permission when prompted
    - Verify: you appear in the participant grid with your avatar and "You" indicator
    - In Window B, click the same voice channel
    - Verify: both users appear in the participant grid
    - Verify: you can hear audio from the other window (speak into mic or play sound)

    **Test 2 — Mute/deafen (VOICE-01):**
    - In Window A, click the mute button in the bottom-left controls
    - Verify: mic icon turns red, Window B sees a mute badge on your tile
    - Verify: Window B can no longer hear audio from Window A
    - Unmute and verify audio resumes
    - Click deafen in Window A
    - Verify: headphone icon turns red and mic is also muted
    - Verify: Window A cannot hear audio from Window B

    **Test 3 — Camera (VOICE-02):**
    - In Window A, click the camera button
    - Grant camera permission when prompted
    - Verify: your avatar tile switches to live video feed
    - Verify: Window B sees your video feed in your tile
    - Toggle camera off
    - Verify: tile reverts to avatar

    **Test 4 — Screen share (VOICE-03):**
    - In Window A, click the screen share button
    - Select a window or screen in the browser picker
    - Verify: a larger tile appears showing the shared screen content
    - Verify: Window B sees the screen share as a larger tile
    - Click "Stop sharing" in the browser's sharing indicator
    - Verify: screen share tile disappears from both windows

    **Test 5 — Voice activity (VOICE-04):**
    - Speak into the mic in Window A
    - Verify: your avatar tile shows a pulsing animation with green border glow
    - Verify: Window B sees the same speaking indicator on your tile
    - Stop speaking and verify the indicator stops within ~2 seconds

    **Test 6 — Navigation and PiP:**
    - While in a voice channel, navigate to a text channel
    - Verify: a floating PiP window appears showing mini participant view
    - Click the PiP to return to the voice channel view

    **Test 7 — Connection quality:**
    - While connected, check the connection indicator in the bottom-left
    - Verify: shows a colored dot (green expected on localhost)
    - Click the indicator
    - Verify: popup shows RTT, packet loss, codec, connection type

    **Test 8 — Auto-switch:**
    - While in voice channel A, click voice channel B
    - Verify: you leave channel A and join channel B seamlessly

    **Test 9 — Disconnect:**
    - Click the disconnect button (red phone icon)
    - Verify: you leave the voice channel, other user sees you leave

    Type "approved" if all 9 scenarios pass, or describe issues found.
  </verify>
  <done>All 9 verification scenarios pass and user types "approved"</done>
</task>

</tasks>

<verification>
All 6 Phase 5 success criteria from ROADMAP.md verified manually:
1. WebRTC P2P audio works through Coturn TURN relay
2. Mute/deafen changes visible to other participants
3. Camera toggle works and is visible to others
4. Screen share works and is visible to others
5. Voice activity indicator lights up in real-time
6. ICE candidate exchange deferred until after join (no IP leak before acceptance)
</verification>

<success_criteria>
- All 9 verification scenarios pass
- User types "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/05-voice-and-video/05-07-SUMMARY.md`
</output>
